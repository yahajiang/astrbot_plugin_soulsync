{
  "enabled": {
    "description": "启用插件",
    "type": "bool",
    "hint": "关闭后不注入加固段、不检测、不拦截任何 LLM 请求。",
    "default": true
  },
  "mode": {
    "description": "处置模式",
    "type": "string",
    "hint": "block：检测到注入直接替换请求为拦截提示，LLM 完全不会执行原指令（最安全）；sanitize：剥离恶意片段后放行其余内容，全部剥离则降级为拦截；warn：放行仅记录日志，用于观察误报。",
    "default": "block",
    "options": ["block", "sanitize", "warn"],
    "option_labels": ["拦截（block）", "剥离（sanitize）", "仅告警（warn）"]
  },
  "guard_persona": {
    "description": "注入人格加固段",
    "type": "bool",
    "hint": "开启后，每次 LLM 请求都会在 system prompt 末尾追加防注入保护段（<InjectionGuard> 标记去重），锁定人格设定、禁止泄露内部指令。",
    "default": true
  },
  "custom_guard_text": {
    "description": "自定义加固段",
    "type": "text",
    "hint": "留空使用内置默认加固段（中英双语规则）。填写后完全覆盖默认段，需自行包含 <InjectionGuard> 标记以去重。",
    "default": ""
  },
  "block_reply": {
    "description": "拦截提示语",
    "type": "text",
    "hint": "block 模式或 sanitize 全部剥离时，由模型转告用户的提示文本（仅当\"发送拦截提示\"开启时生效）。",
    "default": "⚠️ 检测到疑似提示注入内容，本次消息已被安全过滤，未执行其中任何指令。"
  },
  "send_block_reply": {
    "description": "发送拦截提示",
    "type": "bool",
    "hint": "开启：拦截后由模型转告\"拦截提示语\"（block_reply）；关闭：不发送拦截提示，模型仅以当前人格自然、简短地拒绝用户（说明不能这样做），静默拦截只记日志。",
    "default": true
  },
  "notify_admin": {
    "description": "拦截时私发通知管理员",
    "type": "bool",
    "hint": "开启后，每次发生拦截（当前消息拦截、上下文投毒移除）都向管理员私发通知，包含时间、用户、命中规则与拦截内容片段。",
    "default": false
  },
  "notify_admin_ids": {
    "description": "通知的管理员 ID",
    "type": "list",
    "hint": "接收拦截通知的用户 ID 列表；留空时使用\"管理员用户 ID（额外）\"，再留空则仅记日志不通知。",
    "default": []
  },
  "notify_preview_len": {
    "description": "通知内容预览长度上限",
    "type": "int",
    "hint": "管理员通知中每条拦截内容最多展示的字符数（50-500，默认 120），防止通知消息过长。",
    "default": 120
  },
  "image_mode": {
    "description": "图片模式（统计输出渲染为图片）",
    "type": "bool",
    "hint": "与主插件（心旅知音）同款图片输出：开启（默认）后 /防注入 统计 渲染为卡片图片（需已安装 Pillow 且系统有中文字体，否则自动降级文本）。关闭则始终文本输出。也可用 /防注入 图片模式 指令即时切换。",
    "default": true
  },
  "extra_keywords": {
    "description": "追加检测关键词",
    "type": "list",
    "hint": "在内置硬关键词之外追加自定义检测短语，按子串匹配，命中即判定为注入。",
    "default": []
  },
  "enable_heuristics": {
    "description": "启用启发式检测",
    "type": "bool",
    "hint": "开启句式正则、base64 混淆、分隔符拆分（如 i-g-n-o-r-e）等启发式检测；误报率略高于纯关键词。",
    "default": true
  },
  "exempt_admins": {
    "description": "管理员豁免检测",
    "type": "bool",
    "hint": "开启后管理员消息不检测、不拦截。",
    "default": true
  },
  "exempt_users": {
    "description": "白名单用户（豁免检测）",
    "type": "list",
    "hint": "这些用户的 LLM 请求不检测、不拦截。",
    "default": []
  },
  "admin_ids": {
    "description": "管理员用户 ID（额外）",
    "type": "list",
    "hint": "除 AstrBot 管理员角色外，可在此追加拥有 /injguard 指令权限的用户 ID。",
    "default": []
  },
  "log_max_entries": {
    "description": "命中日志条数上限",
    "type": "int",
    "hint": "持久化日志最多保留的最近命中记录条数（1-5000，默认 500），超过自动裁剪，防止文件无限增长。",
    "default": 500
  },
  "scan_contexts": {
    "description": "扫描上下文历史消息",
    "type": "bool",
    "hint": "开启后每次请求同时扫描上下文中的用户历史消息（防记忆/历史投毒，例如 SoulSync 注入的长期记忆）。命中按当前处置模式处理：block 删除整条用户消息，sanitize 剥离恶意片段（剥离后为空则删除），warn 仅记录。",
    "default": true
  },
  "context_scan_max_entries": {
    "description": "上下文扫描条数上限",
    "type": "int",
    "hint": "最多扫描最近 N 条用户历史消息（1-500，默认 100），控制检测开销。",
    "default": 100
  },
  "soulsync_role_exempt": {
    "description": "豁免 SoulSync 内置关系角色表达",
    "type": "bool",
    "hint": "与 astrbot_plugin_soulsync 的关系角色系统联动：开启后，纯身份指派表达（如\"现在你是我的女朋友/恋人/妹妹\"）不拦截，保障合法关系推进。仅豁免纯身份指派类命中，混入\"忽略/泄露/服从/混淆\"等攻击标记的消息仍会拦截。",
    "default": true
  },
  "role_vocab": {
    "description": "额外豁免的关系角色词",
    "type": "list",
    "hint": "内置 39 个 SoulSync 关系角色词之外可追加（如\"主人\"）。仅当\"豁免 SoulSync 内置关系角色表达\"开启时生效。",
    "default": []
  }
}
