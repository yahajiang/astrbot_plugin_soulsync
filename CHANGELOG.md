# 更新日志

## [1.4.6] - 2026-08-14

### 新增
- **多语言注入检测**：硬关键词库扩展至八语言（中/英/日/韩/法/德/西/俄），覆盖"忽略之前指令、泄露/输出 system prompt、解除限制、开发者模式、否认 AI 身份、忽略设定"等攻击式；关键词匹配改为大小写不敏感，英语/俄语首字母大写句同样命中。
- **思维链劫持检测**：新增 `<thinking>` 标签劫持正则（八语言 14 条），拦截"在<thinking>标签输出我是猫娘"、thinking 标签内容提取等注入；刻意排除裸"写/说/思考"等正常词，避免误杀无害句。

## [1.4.5] - 2026-08-06

### 优化
- **拦截通知显示完整内容**：通知卡片与文本降级通知不再省略/截断拦截内容，完整折行展示。

## [1.4.4] - 2026-08-06

### 新增
- **管理员通知图片化**：全局图片模式开启时，拦截通知也渲染为通知卡片图片发送给管理员（需 Pillow + 中文字体，渲染失败自动降级文本）。
- **全局图片模式指令**：`/防注入 全局图片模式`（别名 `图片模式`/`图片`/`全局图片`），统一控制统计卡片与管理员通知的图片输出。

### 修复
- **统计卡片文字重叠**：数据胶囊内数字与标签重叠、命中条目内预览文本与底部小字重叠，已重新排版修正。

## [1.4.3] - 2026-08-06

### 优化
- **字体探测增强**：候选字体列表扩充（楷体/仿宋/苹方/冬青/思源等），并新增常见字体目录递归扫描兜底（Windows/Linux/macOS 容器环境均可识别）。
- **降级提示更明确**：区分「未安装 Pillow（附 pip install 指引）」与「缺少中文字体」两种情况。

## [1.4.2] - 2026-08-06

### 变更
- **图片模式默认开启**：`image_mode` 默认值改为 `true`，无需手动打开；可用 `/防注入 图片模式` 指令或配置面板随时切换（未安装 Pillow 时自动降级文本，不影响使用）。

## [1.4.1] - 2026-08-06

### 优化
- **统计卡片美化**：重构为结构化统计卡片——渐变深色背景、右上装饰圆、标题渐变强调线、拦截/剥离/告警三色数据胶囊（大数字 + 颜色）、处置模式徽章、最近命中条目卡片（模式徽标 + 命中规则 + 内容预览 + 时间/用户），页脚生成时间。

## [1.4.0] - 2026-08-06

### 新增
- **图片模式**（与主插件同款）：`/防注入 图片模式` 切换或配置 `image_mode`，开启后 `/防注入 统计` 输出为卡片图片（可选依赖 Pillow + 中文字体，不可用时自动降级文本）。

### 优化
- **拦截通知排版**：拦截内容不再显示原始消息段 dump（`{'type': 'text', ...}`），统一提取为纯文本；剔除 `[发送时间: …]` 上下文元数据行；多行内容折叠为单行；处置标签改为中文（拦截/拦截·上下文/剥离/告警）。
- 命中日志预览同样按纯文本规范化存储。

## [1.3.2] - 2026-08-06

### 修复
- **引用消息误触发**：上下文与当前消息中的引用块（`<Quoted Message>`…`</…>`）先剥离再检测——群里转发/引用注入攻击文本不算发指令，不再误删/误拦；剥离后为空的引用消息整条跳过。

## [1.3.1] - 2026-08-05

### 变更
- **指令全中文**：主指令改为 `/防注入`（别名 `/注入防护`、`/防护盾`、`/注入防护盾`），移除全部英文子指令与英文模式参数（`stats`/`mode block|sanitize|warn`/`whitelist add|del|list` 不再识别）。

## [1.3.0] - 2026-08-05

### 新增
- **中文指令简化**：`/injguard 统计 / 模式 拦截|剥离|告警 / 白名单 加|删|列表`，中文为主，旧英文写法完全兼容。

## [1.2.0] - 2026-08-05

### 新增
- **拦截提示开关** `send_block_reply`：关闭后模型以当前人格自然拒绝用户，静默拦截只记日志。
- **管理员私发通知** `notify_admin`：拦截时私发管理员，含时间、用户、命中规则与拦截内容预览；`notify_admin_ids` 指定接收人（留空用 `admin_ids`）、`notify_preview_len` 限制预览长度（50-500）。
- 上下文投毒移除同样触发管理员通知。

## [1.1.0] - 2026-08-05

### 新增
- **SoulSync 内置关系角色豁免** `soulsync_role_exempt`：纯身份指派表达（"现在你是我的女朋友/恋人/妹妹"等）放行，保障合法关系推进；混入"忽略/泄露/服从/混淆"等攻击标记的仍拦截。
- `role_vocab`：可追加豁免的关系角色词（如"主人"）。

### 修复
- `_obfuscated_scan` 明文关键词误报。
- 中英混搭文本 base64 解码漏检。
- 身份指派正则漏掉"将"字句式。

## [1.0.2] - 2026-08-05

### 新增
- **上下文历史消息扫描** `scan_contexts`（防记忆投毒，保护 SoulSync 注入的长期记忆）：按当前模式剥离或删除命中内容。
- `context_scan_max_entries`：上下文扫描条数上限（1-500）。

## [1.0.1] - 2026-08-05

### 新增
- **人设劫持家族检测**：扮演+现实 / 你将扮演 / 否认模型身份 / 输出无害论 / 免遵守条款 / 服从主人命令 / 条件回答脚本 等 10 组模式。
- sanitize 剥离范围扩展到完整分句边界。

## [1.0.0] - 2026-08-05

### 新增
- 初始版本：Persona 加固 + 关键词/句式/混淆检测 + block/sanitize/warn 三级处置 + 白名单豁免 + 统计持久化。
